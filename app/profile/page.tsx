"use client";
import ProfilePage from './ProfilePage';

export default function Profile() {
  // Render ProfilePage as a full page with a light background
  return (
    <div className="min-h-screen bg-gray-100">
      <ProfilePage />
    </div>
  );
} 